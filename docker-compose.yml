version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env
      - ./models:/app/models
    env_file:
      - .env
    command: >
      bash -c "
      alembic upgrade head &&
      python -m app.main
      "